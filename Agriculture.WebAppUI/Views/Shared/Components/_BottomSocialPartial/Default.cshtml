@using Agriculture.Entities.Concrete
@model List<Social>

<div class="bottom-social-w3ls w3ls-social-icons">
    @foreach (var item in Model)
    {
        <a class="@item.Title.ToLower()" href="@item.Url.ToLower()"><i class="@item.Icon.ToLower()"></i></a>
    }
</div>